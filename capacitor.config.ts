import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DI04_TareaEvaluativa_Dashboard',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
